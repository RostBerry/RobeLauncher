namespace Core {
    public enum GameState {
        Running,
        WhiteWon,
        BlackWon,
        Draw
    }

    public enum GameMode {
        PvP, 
        PvB,
        BvP,
        BvB
    }
}